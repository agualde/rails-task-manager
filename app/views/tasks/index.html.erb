<h1>Tasks</h1>

<ul>
<% @tasks.each do |task| %>

<li> <%= task.completed ? "✅" : "❌" %>
<%= link_to task.title, task_path(task), class: :btn %> - <%= link_to "🗑️", destroy_path(task), method: :delete, data: { confirm: "Are you sure?"},class: :btn%>
</li>
<% end %>
</ul>

<%= link_to "Add new task", new_path, class: :btn %>
